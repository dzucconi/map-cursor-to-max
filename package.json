{
  "name": "map-cursor-to-max",
  "version": "0.0.0-development",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && tsc -p .",
    "clean": "rm -rf dist",
    "prepublishOnly": "yarn build",
    "test": "jest",
    "semantic-release": "semantic-release"
  },
  "devDependencies": {
    "@types/jest": "25.2.3",
    "jest": "25.1.0",
    "ts-jest": "25.2.1",
    "typescript": "3.8.3",
    "semantic-release": "17.0.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dzucconi/map-cursor-to-max.git"
  }
}
